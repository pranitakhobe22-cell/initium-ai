{
  "name": "initium-ai-orchestration",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend-react && npm run dev",
    "build": "cd frontend-react && npm install && npx vite build --outDir ../dist",

    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "setup": "powershell -ExecutionPolicy Bypass -File setup-initium.ps1"

  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.6.2",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }

}
